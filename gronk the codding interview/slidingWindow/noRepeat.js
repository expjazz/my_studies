const noRepeat = (str) => {
  let windowStart = 0;
  const hash = {};
  let result = 0;
  for (let windowEnd = 0; windowEnd < str.length; windowEnd++) {
    const leftChar = str[windowEnd];
    if (Object.keys(hash).includes())
  }
};
